from django.urls import path
# from .views import MatchView

urlpatterns = [
    # path('', MatchView.as_view({'get': 'list', 'post': 'create'})),
    # path('<int:pk>/', MatchView.as_view({'put': 'update', 'patch': 'partial_update', 'delete': 'destroy'})),
    # path('<int:pk>/finish/', MatchView.as_view({'post': 'finish'})),
    # path('<int:pk>/score/', MatchView.as_view({'post': 'score'})),
    # path('<int:pk>/winner/', MatchView.as_view({'post': 'winner'})),
]
